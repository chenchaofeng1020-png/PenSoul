这是一个**显示与配置不一致**的问题。

**事实情况：**
1.  **底层模型**：您的系统实际连接的是 **火山引擎 (Volcengine)**（在 `.env` 中配置），使用的是豆包或 DeepSeek 模型。
2.  **显示原因**：我在刚刚初始化 `topic_generator` Agent 时，为了兼容性，在数据库中写入了默认值 `model: "gpt-3.5-turbo"`。这导致前端界面（读取数据库配置）直接显示了 "gpt-3.5-turbo"。
3.  **潜在风险**：如果火山引擎不兼容 `gpt-3.5-turbo` 这个模型名称，实际调用可能会失败。即使能调用，界面显示也是误导性的。

**修复计划：**
我将修正后端代码，确保显示和实际调用一致：

1.  **修正数据库默认值**：
    *   将初始化逻辑中的默认模型从 `gpt-3.5-turbo` 改为 `doubao-pro-1.5`（对应前端显示的“豆包 1.5 Pro”）。
    *   添加逻辑，如果发现数据库中仍是旧的 `gpt-3.5-turbo`，自动将其更新为 `doubao-pro-1.5`。

2.  **修正接口调用逻辑**：
    *   在 `generate-topic` 接口中添加映射逻辑：无论前端/数据库传递的是 `doubao-pro-1.5` 还是 `gpt-3.5-turbo`，在向火山引擎发起请求时，都统一替换为环境变量中配置的真实 `VOLCENGINE_MODEL_ID`（Endpoint ID）。

这样修改后，刷新页面您将看到模型变为“豆包 1.5 Pro”，且后台调用会更加稳定。请确认执行。