根据您的需求，我将对“热点内容”模块进行以下精简和重命名操作：

### 1. 菜单重命名
*   将侧边栏（Sidebar）中的 **“热点内容”** 更改为 **“热点雷达”**。
*   同步更新 `App.jsx` 中的路由判断逻辑，确保导航正常工作。

### 2. 功能精简（移除右侧分析板块）
*   **重构 `TrendRadarPage.jsx`**：
    *   移除原本的左右分栏布局（Flex Row）。
    *   删除右侧的 `TrendDetailView` 组件及其相关逻辑（如 `selectedTrend` 状态）。
    *   **新布局**：将左侧的 `TrendList` 改为全宽显示（`w-full`），使其占据整个页面内容区。
*   **移除分析入口**：
    *   在 `TrendList` 组件中，原本点击列表项会触发“选中并展示右侧详情”的操作，现在需要移除该点击事件，或者将其改为直接跳转到原始链接（如果适用），或者仅仅作为纯展示列表。
    *   检查并移除 `TrendList` 中可能存在的“生成选题”、“关联分析”等操作按钮。

### 3. 预期效果
*   点击侧边栏的“热点雷达”，页面将直接展示一个宽敞、全屏的热点列表。
*   用户只能浏览热点本身（标题、来源、热度等），不再有右侧的 AI 深度分析、结合点挖掘或选题生成功能。
*   界面将变得更加纯粹，专注于信息聚合。

我将按照这个计划分步实施，先改名，再动刀砍功能。
