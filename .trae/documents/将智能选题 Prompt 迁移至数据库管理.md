经过排查，您在“系统设置”中看不到变化的原因是：**“系统设置”管理的是存储在数据库中的通用 Agent（如灵感导师），而“智能选题”功能的 Prompt 目前是硬编码在后端代码中的。**

我刚刚优化的 `generate-topic` 接口直接使用了代码中写死的 `systemPrompt` 字符串，它**没有**去读取数据库中的 Agent 配置，因此系统设置页面（读取的是数据库）无法显示或管理这个 Prompt。

**修复方案：**
为了让您能在系统设置中看到并管理这个 Prompt，我将进行以下改造：

1.  **迁移 Prompt 到数据库**：
    *   在后端初始化逻辑中，检查数据库（`agents` 表）是否有名为 `topic_generator` 的 Agent。
    *   如果没有，自动创建一个，并将我刚刚优化过的 Prompt 写入数据库。
    *   如果有，则使用新 Prompt 更新它（确保您的优化生效）。

2.  **修改接口逻辑**：
    *   修改 `/api/trends/generate-topic` 接口，使其优先通过 `getAgentPrompt('topic_generator', ...)` 从数据库获取 Prompt。
    *   这样，接口逻辑将变为：`数据库配置 > 代码默认值`。

3.  **支持动态变量**：
    *   确保从数据库读取的 Prompt 支持 `{{mode}}` 和 `{{materials}}` 这样的变量替换，以便在运行时填入真实的加工模式和素材内容。

**预期效果：**
*   执行后，您刷新“系统设置”页面，将能看到一个新的 `topic_generator`（智能选题助手）Agent。
*   其中的 Prompt 将是我刚刚优化过的版本。
*   未来您直接在前端修改这个 Prompt，智能选题功能也会立即生效。

请确认执行。