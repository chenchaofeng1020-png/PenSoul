我将尝试使用 Supabase JS 客户端直接在代码中执行 SQL 或调用 RPC 来应用这个更改，或者通过重新加载 Schema 缓存来解决这个问题（如果是因为 Schema 缓存过期）。

由于我没有直接执行 SQL 迁移的权限（通常这需要通过 Supabase Dashboard 操作），但我可以尝试一个变通方法：通过服务端代码检查并添加列（如果权限允许），或者提示用户必须手动操作。

鉴于您刚刚已经收到了手动操作的提示，如果手动操作不可行，我可以尝试在 `server/index.cjs` 中添加一段启动逻辑，尝试通过 Postgres 客户端直接执行这段 SQL（如果配置了直连）。

但最快的方法是：**请确认您是否已经在 Supabase Dashboard 中执行了刚才的 SQL 语句？** 如果没有，请先执行。如果已经执行但仍然报错，那可能是因为 Supabase 客户端的 Schema 缓存没有更新。

如果是缓存问题，我会尝试重启后端服务来强制刷新 Schema 缓存。

### 计划：
1.  **重启后端服务**：这通常能强制 Supabase 客户端重新获取最新的 Schema 定义。
2.  **（备选）代码内修复**：如果重启无效，我将在 `server/index.cjs` 中添加一段临时的 raw SQL 执行逻辑（如果环境允许）来尝试自动修复，但这通常需要更高的数据库权限。

让我们先从重启服务开始，因为这最安全且通常有效。